[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "cd client && npm install && npm run build",
  "cd server && pip install -r requirements.txt"
]

[start]
cmd = "cd server && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
